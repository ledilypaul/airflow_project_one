[tool.poetry]
name = "airflow-project-one"
version = "0.1.0"
description = ""
authors = ["ledilypaul <paul.ledily@hotmail.fr>"]
readme = "README.md"
packages = [
    { include = "pipeline" },
    { include = "utils" },
    { include = "sandbox" },
    { include = "utils"}
]

[tool.poetry.dependencies]
python = "^3.12"
pyspark = "^4.0.0"
pyyaml = "^6.0.2"
pytest = "^8.4.1"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
